# Task 4: Flutter `ManagePaymentScreen`, Webview & UI Dialogs

> **Rule Compliance:** 100% compliant with `.agents/rules/design-system.md`, `flutter-architecture.md`, `testing.md`, and `naming-convention.md`.

**Files:**
- Create: `baktaz_flutter/lib/features/account/presentation/views/screens/manage_payment_screen.dart`
- Create: `baktaz_flutter/lib/features/account/presentation/widgets/dialogs/edit_payout_destination_dialog.dart`
- Create: `baktaz_flutter/lib/features/account/presentation/widgets/hitpay_tokenize_webview.dart`
- Create: `baktaz_flutter/test/widget/account/manage_payment_screen_test.dart`

- [ ] **Step 1: Create `EditPayoutDestinationDialog` component in `presentation/widgets/dialogs/`**
  Add client-side regex structural validation (`GCash`: `^09\d{9}$`, `Maya`: `^09\d{9}$`, `Bank`: `^\d{10,16}$`). Use `BaktazTextField` for inputs. Show `ConfirmationDialog` warning before replacing existing destination (`BaktazCard` wrapper).
- [ ] **Step 2: Create `HitPayTokenizeWebview` component in `presentation/widgets/`**
  Intercept redirect URL matching `https://baktaz.app/payment/token-callback?token=...&type=...&last4=...&brand=...` via `onNavigationRequest`, extract query params, pop webview, and trigger `ManagePaymentCubit.savePaymentMethodToken(...)`.
- [ ] **Step 3: Implement `ManagePaymentScreen` view in `presentation/views/screens/`**
  - Use `BaktazCard` and `BaktazSectionHeader` for layout structure.
  - Render saved methods and active `PayoutDestination` within `BaktazCard` items.
  - Disable "+ Add Payment Method" (`BaktazButton`) with a `BaktazStatusBadge` reading `"Max 5 methods"` (`context.l10n.maxSavedMethodsReached`) when `savedMethods.length >= 5`.
  - All text elements mapped to `context.l10n.*` keys (zero hardcoded strings).
- [ ] **Step 4: Add paid challenge missing payout destination warning dialog handler on `/account/payment`**
  Show warning dialog (`context.l10n.payoutDestinationRequiredForPaidChallenge`) when navigating from blocked challenge action.
- [ ] **Step 5: Register GoRouter route `/account/payment`**
  Use `@TypedGoRoute` (`.agents/rules/flutter-architecture.md`).
- [ ] **Step 6: Implementation-First Requirement & Widget / Golden Tests**
  *Note: Implementation MUST be complete before writing widget tests (`.agents/rules/testing.md`).*
  Write widget and golden tests in `test/widget/account/manage_payment_screen_test.dart` using Alchemist (15% tolerance):
  - **Max Methods UI State**: Verify `BaktazButton` is disabled and `BaktazStatusBadge` is visible.
  - **HitPay Navigation Intercept**: Verify webview `onNavigationRequest` extracts params and calls cubit.
  - **Overwrite Warning Dialog**: Verify `ConfirmationDialog` is presented.
  - **Paid Challenge Block Dialog**: Verify warning dialog redirects to `/account/payment`.
  - Store golden files in `test/widget/account/goldens/` with `macos` and `ci` subfolders.
- [ ] **Step 7: Run widget & golden tests**
  Run: `cd baktaz_flutter && flutter test test/widget/account/manage_payment_screen_test.dart`
