# Task 5: Test Execution, Coverage & Pre-Merge Finalization

> **Rule Compliance:** 100% compliant with `.agents/rules/testing.md`.

**Files:**
- Ensure no modifications required (all test files established in steps 1-4).

- [ ] **Step 1: Verify Server Integration Tests Pass**
  Run full server test suite for account feature to ensure all endpoints, validations, and transactions function correctly.
  Command: `cd baktaz_server && fvm dart test test/integration/features/account/manage_payment_endpoint_test.dart --concurrency=1`
- [ ] **Step 2: Verify Flutter Unit Tests Pass**
  Run full cubit unit test suite to ensure `ManagePaymentCubit` correctly orchestrates `AccountRepository` and `FailureHandler`.
  Command: `cd baktaz_flutter && flutter test test/unit/manage_payment_cubit_test.dart`
- [ ] **Step 3: Verify Flutter Widget/Golden Tests Pass**
  Run golden tests with Alchemist to ensure UI renders according to the specification without visual regressions.
  Command: `cd baktaz_flutter && flutter test test/widget/account/manage_payment_screen_test.dart`
- [ ] **Step 4: Run Coverage Analysis**
  Verify that target files meet the coverage threshold (100% for Cubits/Repos, 80% for Widgets).
  Command: `cd baktaz_flutter && flutter test --coverage`
  Inspect `coverage/lcov.info` to validate metrics.
- [ ] **Step 5: Update Documentation & Status**
  Update the master overview `docs/superpowers/specs/manage_payment/00-overview.md` and `docs/superpowers/plans/manage_payment/00-overview.md` to reflect completion. Ensure all parent specs (`docs/superpowers/specs/account/00-overview.md`) are updated to indicate the `ManagePaymentScreen` is no longer deferred.
